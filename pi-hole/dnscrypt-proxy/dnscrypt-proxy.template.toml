# ============================================
# DNSCRYPT-PROXY CONFIGURATION TEMPLATE
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to dnscrypt-proxy.toml
# 2. Replace all values marked <YOUR_...> with your choices
# 3. Find server stamps from the public list at:
#    https://github.com/DNSCrypt/dnscrypt-resolvers/blob/master/v3/public-resolvers.md
#
# IMPORTANT:
# - DO NOT use the actual stamps from this template!
# - Choose relays closest to your geographical location
# ============================================

# ============================================
# LISTEN & NETWORK
# ============================================
listen_addresses = ['0.0.0.0:5053']  # Default, adjust if needed
max_clients = 250
ipv4_servers = true
ipv6_servers = false  # Set to true if you have IPv6
force_tcp = false
timeout = 5000
keepalive = 30

# ============================================
# SERVER SELECTION
# ============================================
dnscrypt_servers = true
doh_servers = true
lb_estimator = true
lb_strategy = 'p2'  # p2 = latency-based, p = random, ph = prefer last

# === FALLBACK RESOLVER ===
# Choose one fallback resolver:
fallback_resolver = '9.9.9.9:8443'  # Quad9
# fallback_resolver = '1.1.1.1:8443'  # Cloudflare

ignore_system_dns = true

# === SERVER NAMES ===
# Replace with your preferred servers from the public list
server_names = [
    '<YOUR_PREFERRED_SERVER_1>',
    '<YOUR_PREFERRED_SERVER_2>',
    '<YOUR_PREFERRED_SERVER_3>',
    # Add more servers if needed
]

# ============================================
# SECURITY & PRIVACY
# ============================================
require_dnssec = true
require_nolog = true
require_nofilter = true

dnscrypt_ephemeral_keys = true
tls_disable_session_tickets = true
tls_cipher_suite = [4865, 4866, 4867]
cert_refresh_delay = 240
edns_payload_size = 1452

# ============================================
# LOGGING
# ============================================
log_level = 0  # 0 = Error, 1 = Warning, 2 = Info, 3 = Debug
use_syslog = true
log_file = '/var/log/dnscrypt-proxy.log'

# ============================================
# STATIC SERVERS
# ============================================
# Replace with your own server stamps from:
# https://github.com/DNSCrypt/dnscrypt-resolvers/blob/master/v3/public-resolvers.md
#
# Correct format example:
# [static.'your-server-name']
# stamp = 'sdns://...'  # Copy stamp from the public list

[static]
  # === EXAMPLE SERVER 1 ===
  # [static.'your-server-1']
  # stamp = 'sdns://<YOUR_STAMP_HERE>'

  # === EXAMPLE SERVER 2 ===
  # [static.'your-server-2']
  # stamp = 'sdns://<YOUR_STAMP_HERE>'

# ============================================
# SOURCES - Public Resolvers (Auto-update)
# ============================================
[sources.'public-resolvers']
urls = [
    'https://raw.githubusercontent.com/DNSCrypt/dnscrypt-resolvers/master/v3/public-resolvers.md',
    'https://download.dnscrypt.info/resolvers-list/v3/public-resolvers.md',
    'https://cdn.jsdelivr.net/gh/DNSCrypt/dnscrypt-resolvers@master/v3/public-resolvers.md'
]
minisign_key = 'RWQf6LRCGA9i53mlYecO4IzT51TGPpvWucNSCh1CBM0QTaLn73Y7GFO3'
cache_file = 'public-resolvers.md'
refresh_delay = 72  # Hours

# ============================================
# CACHE
# ============================================
[cache]
size = 20000  # Maximum cache entries
min_ttl = 600  # 10 minutes
max_ttl = 86400  # 24 hours
negative_min_ttl = 60  # 1 minute
negative_max_ttl = 1800  # 30 minutes
prefetch = true
prefetch_ttl = 7200  # 2 hours

# ============================================
# QUERY LOGGING
# ============================================
[query_log]
file = '/dev/null'  # Disable logging for privacy
format = 'tsv'

# ============================================
# BLOCKING
# ============================================
[blocking]
block_ipv6 = false  # Set to true to block IPv6

# ============================================
# ANONYMIZED DNS (OPTIONAL)
# ============================================
# This feature allows anonymization via relay servers.
# HOW TO USE:
# 1. Choose relay servers from the public list
# 2. Ensure relay servers are added to [static]
# 3. Uncomment and adjust the configuration below
# 4. Replace 'your-resolver' and 'your-relay' with your servers

# [anonymized_dns]
# routes = [
#     { server_name='your-resolver-1', via=['your-relay-1', 'your-relay-2'] },
#     { server_name='your-resolver-2', via=['your-relay-1', 'your-relay-2'] },
# ]

# ============================================
# FORWARDING RULES (OPTIONAL)
# ============================================
[forwarding]
# Use for specific routing to specific servers
# Example:
# '*' = '9.9.9.9'  # Forward all to Quad9
# '*.local' = '192.168.1.1'  # Forward local domains to internal DNS