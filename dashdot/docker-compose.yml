services:
  dash:
    image: mauricenino/dashdot:latest
    restart: unless-stopped
    environment:
      DASHDOT_ENABLE_CPU_TEMPS: "true"
    privileged: true
    ports:
      - "127.0.0.1:3001:3001"
    volumes:
      - /:/mnt/host:ro
