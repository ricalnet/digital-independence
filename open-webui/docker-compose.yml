services:
  # ============================================
  # OLLAMA SERVICE (LOCAL LLM SERVER)
  # Uncomment this section to enable local Ollama instance
  # ============================================
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: ollama
  #   restart: unless-stopped
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_data:/root/.ollama
  #   environment:
  #     - OLLAMA_HOST=${OLLAMA_HOST:-0.0.0.0}
  #     - OLLAMA_KEEP_ALIVE=${OLLAMA_KEEP_ALIVE:-5m}
  #     - OLLAMA_NUM_PARALLEL=${OLLAMA_NUM_PARALLEL:-1}
  #     - OLLAMA_MAX_LOADED_MODELS=${OLLAMA_MAX_LOADED_MODELS:-1}
  #     - OLLAMA_NUM_THREADS=${OLLAMA_NUM_THREADS:-4}
  #     - OLLAMA_GPU_OVERHEAD=${OLLAMA_GPU_OVERHEAD}
  #   logging:
  #     driver: "json-file"
  #     options:
  #       max-size: "${LOG_LEVEL}"
  #       max-size: "${MAX_LOG_SIZE:-10m}"
  #       max-file: "${MAX_LOG_FILES:-3}"

  # ============================================
  # OPEN WEBUI SERVICE (CHAT INTERFACE)
  # ============================================
  openwebui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    restart: always
    ports:
      - "127.0.0.1:3000:8080"
    volumes:
      - openwebui-data:/app/backend/data
    environment:
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://ollama:11434}
      # - HF_TOKEN=${HF_TOKEN}
      - WEBUI_SECRET_KEY=${WEBUI_SECRET_KEY}
      - WEBUI_URL=${WEBUI_URL}
      - WEBUI_AUTH=${WEBUI_AUTH}
      - OAUTH_CLIENT_ID=${OAUTH_CLIENT_ID}
      - OAUTH_CLIENT_SECRET=${OAUTH_CLIENT_SECRET}
      - OAUTH_PROVIDER_NAME=${OAUTH_PROVIDER_NAME}
      - OPENID_PROVIDER_URL=${OPENID_PROVIDER_URL}
      - OPENID_REDIRECT_URI=${OPENID_REDIRECT_URI}
      - ENABLE_OAUTH_SIGNUP=${ENABLE_OAUTH_SIGNUP}
      - ENABLE_LOGIN_FORM=${ENABLE_LOGIN_FORM}
      - ENABLE_PASSWORD_AUTH=${ENABLE_PASSWORD_AUTH}
      - OAUTH_MERGE_ACCOUNTS_BY_EMAIL=${OAUTH_MERGE_ACCOUNTS_BY_EMAIL}
      - CORS_ALLOW_ORIGIN=${CORS_ALLOW_ORIGIN}
    # depends_on:
    #   - ollama
    logging:
      driver: "json-file"
      options:
        max-size: "${MAX_LOG_SIZE:-10m}"
        max-file: "${MAX_LOG_FILES:-3}"

volumes:
  # ============================================
  # DATA VOLUMES
  # Uncomment ollama_data if Ollama service is enabled
  # ============================================
  # ollama_data:
  #   driver: local

  openwebui-data:
    driver: local
